{
  "dependencies": {
    "socket.io-client": "^4.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd server && nodemon --config nodemon.json src/app.ts",
    "server": "cd server && node src/App.ts",
    
    "buildServer": "cd server && yarn install"
  },
  "devDependencies": {
    "@types/react": "^17.0.43",
    "typescript": "^4.6.3"
  }
}
